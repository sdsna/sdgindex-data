/**
 * Get all regions from the dataStore.
 * @param {Object} dataStore - The store where the data are loaded
 * @return {Array} return an array of regions
 */
export const getRegions = (dataStore) => dataStore.regions;
